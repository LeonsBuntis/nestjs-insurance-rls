# JWT signing secret — change before deploying to any shared environment
JWT_SECRET=changeme

# Set to true to mount POST /auth/token (local development only — never enable in production)
MOCK_AUTH_ENABLED=false

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=insurance
APP_DB_USER=app_user
APP_DB_PASSWORD=app_password
